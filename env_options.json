{
  "environment_variables": [
    {
      "name": "TELEGRAM_BOT_TOKEN",
      "type": "string",
      "default": "",
      "description": "Токен бота Telegram, полученный от @BotFather. Обязательный параметр.",
      "required": true,
      "validation": {
        "min_length": 40,
        "pattern": "^[0-9]+:[a-zA-Z0-9_-]+$"
      },
      "example": "123456789:ABCdefGHIjklMNOpqrsTUVwxyz1234567890"
    },
    {
      "name": "TELEGRAM_API_BASE_URL",
      "type": "string",
      "default": "https://api.telegram.org",
      "description": "Базовый URL для Telegram Bot API. Измените для использования локального Bot API сервера.",
      "required": false,
      "validation": {
        "format": "url"
      },
      "example": "https://api.telegram.org"
    },
    {
      "name": "PORT",
      "type": "integer",
      "default": 8000,
      "description": "Порт для запуска MCP сервера",
      "required": false,
      "validation": {
        "min": 1024,
        "max": 65535
      },
      "example": 8000
    },
    {
      "name": "HOST",
      "type": "string",
      "default": "0.0.0.0",
      "description": "Хост для запуска MCP сервера",
      "required": false,
      "validation": {
        "pattern": "^[a-zA-Z0-9\\.\\-]+$"
      },
      "example": "0.0.0.0"
    },
    {
      "name": "LOG_LEVEL",
      "type": "string",
      "default": "INFO",
      "description": "Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)",
      "required": false,
      "validation": {
        "enum": ["DEBUG", "INFO", "WARNING", "ERROR", "CRITICAL"]
      },
      "example": "INFO"
    },
    {
      "name": "MAX_MESSAGE_LENGTH",
      "type": "integer",
      "default": 4096,
      "description": "Максимальная длина сообщения в Telegram (1-4096)",
      "required": false,
      "validation": {
        "min": 1,
        "max": 4096
      },
      "example": 4096
    },
    {
      "name": "DEFAULT_PARSE_MODE",
      "type": "string",
      "default": "HTML",
      "description": "Режим парсинга по умолчанию для сообщений",
      "required": false,
      "validation": {
        "enum": ["HTML", "Markdown", "MarkdownV2"]
      },
      "example": "HTML"
    },
    {
      "name": "REQUEST_TIMEOUT",
      "type": "integer",
      "default": 30,
      "description": "Таймаут запросов к Telegram API в секундах (5-120)",
      "required": false,
      "validation": {
        "min": 5,
        "max": 120
      },
      "example": 30
    }
  ],
  "metadata": {
    "version": "1.0.0",
    "description": "Описание переменных окружения для MCP Telegram Server",
    "usage": {
      "docker": "Передавайте переменные через -e или --env-file",
      "local": "Создайте файл .env в корне проекта или экспортируйте переменные в shell",
      "example": ".env.example содержит примеры всех переменных"
    },
    "notes": [
      "TELEGRAM_BOT_TOKEN — обязательный параметр",
      "Получите токен от @BotFather в Telegram",
      "Для локального Bot API сервера измените TELEGRAM_API_BASE_URL"
    ]
  }
}
